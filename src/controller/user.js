/**
 * @description User路由controller
 * @author king
 */

const { createUser } = require('../service/user')

async function register(
    {
        userName,
        password,
        email,
        avator,
        isAdmin
    }
) {
    
}